# ══════════════════════════════════════════════════════════════
# TrafficGuard AI Service — PINNED Dependencies (March 2026)
# ══════════════════════════════════════════════════════════════
# DO NOT change these versions.  The detection thresholds in
# lightweight_analyzer.py were calibrated and validated against
# these exact library versions.  Upgrading any package — even a
# minor/patch bump — can silently change pixel values, contour
# detection, or HSV conversion behaviour, which will break the
# trained fire / accident / traffic-jam classifiers.
#
# Validated: 59/59 real videos + 12 synthetic + 5 concurrent
# Fire: 18/18 | TJ: 13/13 | Accident: 28/28
# Python: 3.13.x
# ══════════════════════════════════════════════════════════════

opencv-python-headless==4.12.0.88
numpy==2.2.6
fastapi==0.118.0
uvicorn==0.38.0
pillow==12.0.0
python-multipart==0.0.20
python-dotenv==1.0.1
httpx==0.28.1
aiohttp==3.13.3
